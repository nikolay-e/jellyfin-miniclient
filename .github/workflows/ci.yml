name: CI

on:
  pull_request:
    branches: [ main ]

jobs:
  claude:
    name: Claude
    runs-on: ubuntu-latest
    steps:
      - name: Claude Code Action Official
        uses: anthropics/claude-code-action@v1
